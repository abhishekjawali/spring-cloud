spring:
 application:
  name: myapp-server

server:
 port: 9091

eureka:
 client:
  registerWithEureka: true
  fetchRegistry: true
  serviceUrl:
   defaultZone: http://localhost:8671/eureka/
 instance:
  hostname: localhost